# Шпаргалка по методам массивов JavaScript/TypeScript

## Методы изменения массива (Mutating)

| Метод        | Что делает                         | Пример                                       | Результат                                |
|--------------|------------------------------------|--------------------------------------------- |------------------------------------------|
| `push()`     | Добавляет элементы в конец массива | `arr.push(4, 5)` на `[1,2,3]`                | `[1,2,3,4,5]` (возвращает длину: 5)      |
| `pop()`      | Удаляет последний элемент          | `arr.pop()` на `[1,2,3]`                     | `[1,2]` (возвращает: 3)                  |
| `shift()`    | Удаляет первый элемент             | `arr.shift()` на `[1,2,3]`                   | `[2,3]` (возвращает: 1)                  |
| `unshift()`  | Добавляет элементы в начало        | `arr.unshift(0)` на `[1,2,3]`                | `[0,1,2,3]` (возвращает длину: 4)        |
| `splice()`   | Удаляет/добавляет элементы         | `arr.splice(1, 2, 'a', 'b')` на `[1,2,3,4]`  | `[1,'a','b',4]` (возвращает: [2,3])      |
| `reverse()`  | Переворачивает массив              | `arr.reverse()` на `[1,2,3]`                 | `[3,2,1]`                                |
| `sort()`     | Сортирует массив                   | `arr.sort()` на `[3,1,2]`                    | `[1,2,3]`                                |

## Методы создания нового массива (Non-mutating)

| Метод        | Что делает                        | Пример                             | Результат    |
|--------------|-----------------------------------|------------------------------------|--------------|
| `map()`      | Преобразует каждый элемент        | `[1,2,3].map(x => x * 2)`          | `[2,4,6]`    |
| `filter()`   | Фильтрует элементы по условию     | `[1,2,3,4].filter(x => x > 2)`     | `[3,4]`      |
| `slice()`    | Копирует часть массива            | `[1,2,3,4].slice(1, 3)`            | `[2,3]`      |
| `concat()`   | Объединяет массивы                | `[1,2].concat([3,4])`              | `[1,2,3,4]`  |
| `flat()`     | Разворачивает вложенные массивы   | `[1,[2,[3]]].flat(2)`              | `[1,2,3]`    |
| `flatMap()`  | map() + flat()                    | `[1,2].flatMap(x => [x, x*2])`     | `[1,2,2,4]`  |

## Методы поиска

| Метод            | Что делает                            | Пример                            | Результат |
|------------------|---------------------------------------|-----------------------------------|-----------|
| `find()`         | Находит первый элемент по условию     | `[1,2,3].find(x => x > 1)`        | `2`       |
| `findIndex()`    | Находит индекс первого элемента       | `[1,2,3].findIndex(x => x > 1)`   | `1`       |
| `indexOf()`      | Находит индекс элемента               | `[1,2,3].indexOf(2)`              | `1`       |
| `lastIndexOf()`  | Находит последний индекс элемента     | `[1,2,1].lastIndexOf(1)`          | `2`       |
| `includes()`     | Проверяет наличие элемента            | `[1,2,3].includes(2)`             | `true`    |

## Методы проверки условий

| Метод      | Что делает                        | Пример                               | Результат |
|------------|-----------------------------------|--------------------------------------|-----------|
| `every()`  | Проверяет все элементы            | `[2,4,6].every(x => x % 2 === 0)`    | `true`    |
| `some()`   | Проверяет хотя бы один элемент    | `[1,2,3].some(x => x > 2)`           | `true`    |

## Методы редукции

| Метод            | Что делает                           | Пример                                         | Результат |
|------------------|--------------------------------------|------------------------------------------------|-----------|
| `reduce()`       | Сворачивает массив в одно значение   | `[1,2,3].reduce((acc, x) => acc + x, 0)`       | `6`       |
| `reduceRight()`  | То же, но справа налево              | `[1,2,3].reduceRight((acc, x) => acc + x, 0)`  | `6`       |

## Методы итерации

| Метод        | Что делает                        | Пример                                   | Результат                      |
|--------------|-----------------------------------|------------------------------------------|--------------------------------|
| `forEach()`  | Выполняет функцию для каждого     | `[1,2,3].forEach(x => console.log(x))`   | `undefined` (печатает 1,2,3)   |

## Методы преобразования

| Метод         | Что делает               | Пример                   | Результат   |
|---------------|--------------------------|--------------------------|-------------|
| `join()`      | Объединяет в строку      | `[1,2,3].join('-')`      | `"1-2-3"`   |
| `toString()`  | Преобразует в строку     | `[1,2,3].toString()`     | `"1,2,3"`   |

## Статические методы Array

| Метод              | Что делает                          | Пример                    | Результат       |
|--------------------|-------------------------------------|---------------------------|-----------------|
| `Array.from()`     | Создает массив из итерируемого      | `Array.from('abc')`       | `['a','b','c']` |
| `Array.of()`       | Создает массив из аргументов        | `Array.of(1,2,3)`         | `[1,2,3]`       |
| `Array.isArray()`  | Проверяет, является ли массивом     | `Array.isArray([1,2])`    | `true`          |

## Полезные паттерны

### Удалить дубликаты
```javascript
const unique = [...new Set([1,2,2,3])];  // [1,2,3]
```

### Сортировка чисел
```javascript
[3,1,2].sort((a, b) => a - b);  // [1,2,3] по возрастанию
[3,1,2].sort((a, b) => b - a);  // [3,2,1] по убыванию
```

### Подсчет элементов
```javascript
const count = arr.reduce((acc, val) => {
  acc[val] = (acc[val] || 0) + 1;
  return acc;
}, {});
```

### Группировка элементов
```javascript
const grouped = arr.reduce((acc, val) => {
  const key = val.category;
  acc[key] = acc[key] || [];
  acc[key].push(val);
  return acc;
}, {});
```

### Flatten вложенных массивов
```javascript
const flat = arr.flat(Infinity);  // Разворачивает на любую глубину
```

### Создать массив с числами от 0 до n
```javascript
const range = Array.from({length: 5}, (_, i) => i);  // [0,1,2,3,4]
const range = [...Array(5).keys()];  // [0,1,2,3,4]
```

## Важные замечания

⚠️ **sort()** без компаратора сортирует как строки:
```javascript
[1, 10, 2].sort()  // [1, 10, 2] - неправильно!
[1, 10, 2].sort((a,b) => a - b)  // [1, 2, 10] - правильно
```

⚠️ **Методы изменяют исходный массив**: push, pop, shift, unshift, splice, reverse, sort

✅ **Методы НЕ изменяют исходный массив**: map, filter, slice, concat, flat, flatMap

💡 **Чейнинг методов** (цепочки):
```javascript
[1,2,3,4,5]
  .filter(x => x % 2 === 0)  // [2,4]
  .map(x => x * 2)            // [4,8]
  .reduce((a,b) => a + b, 0); // 12
```
