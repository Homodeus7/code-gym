/**
 * DAY 5: Two Pointers (Два указателя)
 * Уровень: Средний
 */

/**
 * Задача 1: Переместить нули в конец
 *
 * Дан массив nums.
 * Переместите все 0 в конец, сохраняя порядок ненулевых элементов.
 * Нужно модифицировать массив in-place без создания копии.
 *
 * Примеры:
 * moveZeroes([0, 1, 0, 3, 12]) → модифицирует в [1, 3, 12, 0, 0]
 * moveZeroes([0]) → модифицирует в [0]
 * moveZeroes([1, 2, 3]) → модифицирует в [1, 2, 3]
 *
 * Ограничения:
 * - 1 <= nums.length <= 10^4
 * - -2^31 <= nums[i] <= 2^31 - 1
 *
 * Подсказка: Используйте два указателя
 */
function moveZeroes(nums: number[]): void {
  // Ваше решение здесь
  // Функция ничего не возвращает, модифицирует nums
}

/**
 * Задача 2: Объединить отсортированные массивы
 *
 * Даны два отсортированных массива nums1 и nums2.
 * Объедините nums2 в nums1 как один отсортированный массив.
 * nums1 имеет достаточно места (размер m + n) для размещения элементов из nums2.
 *
 * Примеры:
 * merge([1, 2, 3, 0, 0, 0], 3, [2, 5, 6], 3) → модифицирует в [1, 2, 2, 3, 5, 6]
 * merge([1], 1, [], 0) → модифицирует в [1]
 * merge([0], 0, [1], 1) → модифицирует в [1]
 *
 * Ограничения:
 * - nums1.length == m + n
 * - nums2.length == n
 * - 0 <= m, n <= 200
 * - Оба массива отсортированы по возрастанию
 *
 * Подсказка: Начните с конца массивов
 */
function merge(nums1: number[], m: number, nums2: number[], n: number): void {
  // Ваше решение здесь
}

/**
 * Задача 3: Удалить дубликаты из отсортированного массива
 *
 * Дан отсортированный массив nums.
 * Удалите дубликаты in-place так, чтобы каждый элемент был уникален.
 * Верните количество уникальных элементов k.
 * Первые k элементов nums должны содержать уникальные элементы в исходном порядке.
 *
 * Примеры:
 * removeDuplicates([1, 1, 2]) → 2, nums = [1, 2, _]
 * removeDuplicates([0, 0, 1, 1, 1, 2, 2, 3, 3, 4]) → 5, nums = [0, 1, 2, 3, 4, _, _, _, _, _]
 *
 * Ограничения:
 * - 1 <= nums.length <= 3 * 10^4
 * - -100 <= nums[i] <= 100
 * - nums отсортирован по возрастанию
 *
 * Подсказка: Два указателя - один для чтения, один для записи
 */
function removeDuplicatesSorted(nums: number[]): number {
  // Ваше решение здесь
  return 0;
}

/**
 * Задача 4: Сумма двух чисел II (отсортированный массив)
 *
 * Дан отсортированный по возрастанию массив numbers (индексация с 1).
 * Найдите два числа, которые в сумме дают target.
 * Верните индексы этих чисел (с 1).
 * Гарантируется ровно одно решение, нельзя использовать один элемент дважды.
 *
 * Примеры:
 * twoSumSorted([2, 7, 11, 15], 9) → [1, 2]
 * twoSumSorted([2, 3, 4], 6) → [1, 3]
 * twoSumSorted([-1, 0], -1) → [1, 2]
 *
 * Ограничения:
 * - 2 <= numbers.length <= 3 * 10^4
 * - -1000 <= numbers[i] <= 1000
 * - numbers отсортирован по возрастанию
 * - -1000 <= target <= 1000
 *
 * Подсказка: Два указателя с начала и конца
 */
function twoSumSorted(numbers: number[], target: number): number[] {
  // Ваше решение здесь
  return [];
}

// ========================================
// Тесты (раскомментируйте для проверки)
// ========================================

/*
console.log("=== Задача 1: Переместить нули ===");
const arr1 = [0, 1, 0, 3, 12];
moveZeroes(arr1);
console.log(arr1); // [1, 3, 12, 0, 0]

const arr2 = [0];
moveZeroes(arr2);
console.log(arr2); // [0]

console.log("\n=== Задача 2: Объединить массивы ===");
const arr3 = [1, 2, 3, 0, 0, 0];
merge(arr3, 3, [2, 5, 6], 3);
console.log(arr3); // [1, 2, 2, 3, 5, 6]

console.log("\n=== Задача 3: Удалить дубликаты ===");
const arr4 = [1, 1, 2];
console.log(removeDuplicatesSorted(arr4), arr4); // 2, [1, 2, ...]

const arr5 = [0, 0, 1, 1, 1, 2, 2, 3, 3, 4];
console.log(removeDuplicatesSorted(arr5), arr5); // 5, [0, 1, 2, 3, 4, ...]

console.log("\n=== Задача 4: Two Sum II ===");
console.log(twoSumSorted([2, 7, 11, 15], 9)); // [1, 2]
console.log(twoSumSorted([2, 3, 4], 6)); // [1, 3]
*/
